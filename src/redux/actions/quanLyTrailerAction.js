export const tatTrailerAction = (trailerPhim) => {
    return {
        type:'TAT_TRAILER',
        trailerPhim
    }
}

export const chonTrailerAction = (trailerPhim) => {
    return {
        type:'CHON_TRAILER',
        trailerPhim
    }
}